/**
 * Register page — /register route.
 *
 * Renders the RegisterPage feature component.
 * Requirements: 5.1, 9.1, 9.3
 */

import { RegisterPage } from '@/features/auth/register/RegisterPage';

export default function RegisterPageRoute() {
  return <RegisterPage />;
}
