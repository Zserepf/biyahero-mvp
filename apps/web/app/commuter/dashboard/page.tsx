'use client';

/**
 * /commuter/dashboard — Commuter home screen (mobile-first PWA shell)
 */

import { CommuterDashboard } from '@/features/dashboard/commuter/CommuterDashboard';

export default function CommuterDashboardPage() {
  return <CommuterDashboard />;
}
