/**
 * /commuter/fare — Fare calculator (commuter + guest access)
 * Requirements: 2.1, 2.5, 2.9
 */

import { CalculateFarePage } from '@/features/fare/calculate-fare/CalculateFarePage';

export default function CommuterFarePage() {
  return <CalculateFarePage />;
}
